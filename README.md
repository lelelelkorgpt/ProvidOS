# ProvidOS
arch based OS made by a 14 years old with ❤️. made with archiso.
Use the Caelestia shell alonside with astraunot
## To compile
```bash
sudo pacman -Sy archiso
git clone https://github.com/lelelelkorgpt/ProvidOS.git
cd ProvidOS
sudo mkarchiso -v -w src/work/ -o src/out/ src/profile/
```
